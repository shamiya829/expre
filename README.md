# Experiential Real Estate — Static Site

Static multi-page website (no build step).

## Run locally

From the repo root:

```bash
python3 -m http.server 5173
```

Then open `http://localhost:5173`.

